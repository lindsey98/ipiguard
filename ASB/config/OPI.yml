injection_method: observation_prompt_injection
attack_tool:
  - all
  # - agg
  # - non-agg

llms:
  # Locally-served model via vLLM (OpenAI-compatible). Must match the vLLM --served-model-name.
  # Reached through LOCAL_BASE_URL / LOCAL_API_KEY (defaults http://localhost:8000/v1, "EMPTY").
  - Qwen3-30B-A3B-Instruct-2507
  # - gpt-4o-mini
  # - gpt-4o-2024-08-06

attack_types:
  # - combined_attack
  - context_ignoring
  # - fake_completion
  # - escape_characters
  # - naive

# CaMeL defense: code-gen + interpreter over the ASB tools; injected observation text is treated
# as untrusted data and the attacker tool is denied by the ASB security policy. Comment this line
# out to run the undefended baseline (to confirm ASR > 0 first).
defense_type: camel
# Other ASB OPI defenses: delimiters_defense, ob_sandwich_defense, instructional_prevention
# suffix: test
